# SQLite database location used by Prisma (required, no default).
# Must be a file: URL. The app throws on startup if this is unset.
DATABASE_FILE=file:./database.sqlite

# Port the Next.js server listens on (required, no default).
# `pnpm dev` / `pnpm start` fail fast if this is unset.
PORT=3000
